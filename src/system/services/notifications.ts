/**
 * Notifications - Centralized export
 * Point d'entrée unique pour le système de notifications unifié
 */

export { unifiedNotificationService } from './unifiedNotificationService';
export type { NotificationId, NotificationType, NotificationConfig } from './unifiedNotificationService';
